#!/usr/bin/env ansible-playbook
# ansible 2.2.1.0

- name: Configure my dotfiles
  remote_user: garfield
  tasks:
      - name: copy tmux conf file
        copy: src=tmux/tmux.conf dest="/home/{{ my_user}}/.tmux.conf" mode=644