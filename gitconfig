[user]
        name = GaryCarneiro
	email = GaryCarneiro@github.com
[color]
        ui = auto
        ui = true
[push]
	default = simple
[alias]
  	co = checkout
  	ci = commit
	st = status
	br = branch
	l  = log -p
	lo = log --oneline
	parent = "!git show-branch | grep '*' | grep -v \"$(git rev-parse --abbrev-ref HEAD)\" | head -n1 | sed 's/.*\\[\\(.*\\)\\].*/\\1/' | sed 's/[\\^~].*//' #"
  	mom = "!git log --graph --decorate"
